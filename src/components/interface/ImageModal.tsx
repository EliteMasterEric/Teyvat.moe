/**
 * An image modal displays an image in a dialog box rendered over the current page.
 * The background will be tinted, the image will be at the highest available resolution,
 * and clicking outside the dialog will close it and return to the screen behind it.
 */

import _ from 'lodash';

const ImageModal = _.noop();

export default ImageModal;
