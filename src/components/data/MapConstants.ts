/**
 * Contains Leaflet constants safe to define outside of src/components/views/map.
 */

// Center of the map.
export const MAP_CENTER: [number, number] = [-32, 32];

export const DEFAULT_ZOOM = 4;
