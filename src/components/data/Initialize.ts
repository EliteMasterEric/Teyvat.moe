import { initializeAllMapFeatures } from './MapFeatures';

/**
 * Triggers multiple async operations just by importing this file.
 */
const initializeAll = () => {
  initializeAllMapFeatures();
};

initializeAll();
